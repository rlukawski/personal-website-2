// This file should only be used to define types for the root layout
// The actual layouts are in [locale]/layout.tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
